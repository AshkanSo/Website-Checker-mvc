@{
    ViewData["Title"] = "Phone Numbers";
}
<h1>@ViewData["Title"]</h1>



@model IEnumerable<SiteChecker_mvc.Models.PhoneNumbers>

@{
    ViewBag.Title = "Phone Numbers Index";
}


<table class="table">
    <thead>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Number)
        </th>
        <th>
            Web Sites
        </th>
        <th>
            <a asp-controller="PhoneNumbers" asp-action="Create"  class="btn btn-success mx-2">
                <i class="bi bi-pencil-square"></i>
                Create New Phone Number
            </a>
        </th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model) {
        <tr>
            <td>
                @* @Html.DisplayFor(modelItem => item.Name) *@
                @item.PK_PhoneNumber
            </td>
        <td>
            @* @Html.DisplayFor(modelItem => item.Name) *@
            @item.Name
        </td>
        <td>
            @* @Html.DisplayFor(modelItem => item.Number) *@
            @item.Number
        </td>
        <td>
        <td>
            @* @Html.ActionLink("Edit", "Edit", new { id=item.PK_PhoneNumber }) | *@
            <a asp-controller="PhoneNumbers" asp-action="Edit" asp-route-id="@item.PK_PhoneNumber" class="btn btn-primary mx-2">
                <i class="bi bi-pencil-square"></i>
                Edit
            </a>
            @* @Html.ActionLink("Details", "Details", new { id=item.PK_PhoneNumber }) | *@
            <a asp-controller="PhoneNumbers" asp-action="Delete" asp-route-id="@item.PK_PhoneNumber" class="btn btn-danger mx-2">
                <i class="bi bi-pencil-square"></i>
                Delete
            </a>
            @* @Html.ActionLink("Delete", "Delete", new { id=item.PK_PhoneNumber }) *@
        </td>
        @* <ul> *@
        @*     @foreach (var website in item.WebSitesCollection) *@
        @*     { *@
        @*         <li>@Html.DisplayFor(modelItem => website.Name) (@Html.DisplayFor(modelItem => website.Url))</li> *@
        @*     } *@
        @* </ul> *@

    </tr>
    }
    </tbody>
</table>
